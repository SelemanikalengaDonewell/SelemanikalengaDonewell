<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EnvironmentApp3.HealthPages"
             Title="Health Policies">

    <ScrollView>
    <VerticalStackLayout Spacing="10" Padding="10">
            <Frame>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="180"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0"
                           Text="Defintion"
                             HorizontalOptions="Center"
                           TextColor="Blue"
                           FontSize="30"
                           FontAttributes="Bold"/>
                    <Label Grid.Row="1"
                        Text="​Health policy is defined by the World Health Organization as the decisions, plans, and actions that are undertaken to achieve specific healthcare goals within a society.
There are many categories of health policy, such as: Public health, mental health policy, health care services policy, insurance policy, personal healthcare policy, pharmaceutical policy."
                        FontSize="Subtitle"
                           />
                </Grid>
            </Frame>
    </VerticalStackLayout>
    </ScrollView>
</ContentPage>